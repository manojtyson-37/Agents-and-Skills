node_modules/
*.log
.cso/state/
.cso/daemon.log
.cso/meta.log
.cso/dashboard.log
.cso/state/archive/
.claude/launch.json
.claude/settings.local.json

# Secrets — never commit. .mcp.json is now tracked but holds only ${VAR} placeholders.
.env
.env.local
